trigger LanguageCourseTrigger on Language_Course__c (after insert, after update, after delete) {
    // <write your own notification code>    
    // The Sales team wants to be notified when a language course is added or changed,
    // and they want to know who is teaching each course.

    //for (Language_Course__c lc : Trigger.New) {


    //}
}